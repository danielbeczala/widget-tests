{
  "name": "widget-tests",
  "version": "2.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "playwright test",
    "dev": "playwright test --debug",
    "docker:test": "docker run --rm -v $(pwd):/app -w /app playwright-tests"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.48.1",
    "dotenv": "^16.4.5",
    "@types/node": "^22.7.7"
  }
}
